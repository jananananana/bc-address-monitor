import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Ledger extends Component {


  render() {
    return (
        <div className="c-ledger">
          <form action="https://blockchain.info/rawaddr/$bitcoin_address">

          </form>
        </div>
      );
  }

}

// Ledger.PropTypes = {

// };

export default Ledger;
